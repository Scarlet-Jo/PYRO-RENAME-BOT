version: "3.9"
services:
  pyro:
    build: .
    environment:
      API_ID: $API_ID
      API_HASH: $API_HASH
      TG_BOT_TOKEN: $TG_BOT_TOKEN
      FORCE_SUB: $FORCE_SUB
      ADMIN: $ADMIN
      DB_URL: $DATABASE_URI
      DB_NAME: $DATABASE_NAME
      
